[
  {
    "name": "/postnow shows error when no channel configured",
    "steps": [
      {
        "send": { "text": "/postnow" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "⚠️ No channel configured yet. Set a channel first."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "postnow button tap shows error when no channel",
    "steps": [
      {
        "send": { "callback": "postnow:do" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "⚠️ No channel configured yet. Set a channel first."
            }
          }
        ]
      }
    ]
  }
]
